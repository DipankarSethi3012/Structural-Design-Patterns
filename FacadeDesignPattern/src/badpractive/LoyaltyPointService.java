package badpractive;

public class LoyaltyPointService {
    public void addPoints(String accountId, int points) {
        System.out.println(points + " loyalty points to added account " + accountId);
    }
}
